- filename: StaticIv1.js
  description: One path in the conditional initializes the iv with random bytes, whereas the other path generates a static iv, which is insecure. Only the path that generates a static iv is reachable, so this code is not secure.
  secure: no
